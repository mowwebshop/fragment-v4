{
  "name": "fragment-injector-service",
  "version": "1.0.0",
  "description": "HTTP service to inject fp=unsafe&fm=<fragment> into VLESS/Trojan links",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
